additional scripts can be found in ../twip/scripts/

cat_tweets -- combine tweetget json dump files into single large CSV

installed with

    $ pip install -e /path/to/twip

for help:

    $ cat_tweets -h
